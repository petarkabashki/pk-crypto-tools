Tech used is python, pandas, numpy, c-modules
Dev setup is using uvicorn and python 3.11 local environment in the root folder of this project. To run the app, use `python run.py`. To install dependencies, first activate the local uv environment using `source .venv/bin/activate`, then use `uv pip install -r requirements.txt`